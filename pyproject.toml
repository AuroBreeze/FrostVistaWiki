[project]
name = "frostvista-wiki"
version = "0.1.0"
description = "Documentation and learning-path site for FrostVistaOS"
requires-python = ">=3.10"
dependencies = [
    "zensical>=0.0.53",
]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = []
